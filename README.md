# Wedding site

## Запуск

```bash
npm install
npm run dev
```

Архив не содержит `node_modules`, чтобы после распаковки на macOS не возникало ошибок прав доступа. Зависимости устанавливаются локально командой `npm install`.
